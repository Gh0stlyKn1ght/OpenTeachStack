# OpenTeachStack Claude Skills

This directory is a generated compatibility mirror of the canonical project skills in `.agents/skills/`.

Do not hand-edit mirrored `SKILL.md` files here.

Use:

```bash
npm run skills:sync
```

to update the Claude project-skill mirror, and:

```bash
npm run ci:skills
```

to verify parity and required skill metadata.

The canonical source remains `.agents/skills/`.
