export const metadata = {
  title: "Troubleshooting",
  description: "Short fixes for common Teaching Teachers build and classroom workflow problems.",
}

# Troubleshooting

Use this category for broken links, embed issues, missing sources, build failures, and classroom workflow snags.

When something breaks, write down the exact route, error, last change, and verification command before changing the fix.
